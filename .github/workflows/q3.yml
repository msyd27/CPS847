name: deploy code

on: ['push']

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Deploy to server
      uses: appleboy/ssh-action@master
      with:
        host: moon.scs.torontomu.ca
        username: musyed
        password: ${{ secrets.PASSWORD_SSH }}
        script: |
          scp -r index.html musyed@moon.scs.torontomu.ca:/home/musyed/courses/cps847/<musyed>/index.html
